// Local storage keys

export const KEY_GITLAB_API_TOKEN = 'gitlabApiToken';

export const COLUMNS = [
  'workflow::pending',
  'workflow::mock-up',
  'workflow::To discuss',
  'workflow::To discuss (prio)',
  'workflow::Ready',
  'workflow::next sprint',
  'workflow::to do',
  'workflow::doing',
  'workflow::merge',
  'workflow::push to staging',
  'workflow::need review',
  'workflow::failed review',
  'workflow::on hold',
  'workflow::push to prod',
  'workflow::deployed',
];

export const EPIC_NAME = 'EPIC::bankroot';
